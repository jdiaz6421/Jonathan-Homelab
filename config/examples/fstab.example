# Replace the server and export path with values from the Synology NFS configuration.
# x-systemd.automount avoids blocking startup; nofail allows the VM to boot if the NAS is offline.
192.168.50.30:/volume1/homelab-data /mnt/homelab-data nfs defaults,_netdev,nofail,x-systemd.automount 0 0
